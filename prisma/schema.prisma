// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Product {
  id          Int      @id @default(autoincrement())
  name        String
  color       String   @default("")
  description String
  price       Float
  stock       Int      @default(0)
  isVisible   Boolean  @default(true)
  image       String   // Primary image URL
  images      String   @default("[]") // JSON string of an array of extra image URLs
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  
  orderItems  OrderItem[]
}

model Order {
  id                  Int      @id @default(autoincrement())
  firstName           String
  lastName            String
  documentType        String   // V, E, P, etc.
  documentNumber      String
  
  paymentMethod       String   // PAGO_MOVIL, ZELLE, BINANCE
  paymentBank         String?
  paymentPhone        String?
  paymentDestDocument String?
  paymentReference    String
  paymentScreenshot   String?  // URL to uploaded image
  
  shippingMethod      String   @default("PICKUP") // PICKUP, DELIVERY_VALENCIA, ENVIOS_NACIONALES
  shippingZone        String?  // For Valencia Delivery (e.g. El Bosque)
  shippingAgency      String?  // For Envios Nacionales (e.g. MRW)

  totalUsd            Float
  totalBs             Float
  status              String   @default("PENDING") // PENDING, ACCEPTED, REJECTED
  
  createdAt           DateTime @default(now())
  updatedAt           DateTime @updatedAt

  items               OrderItem[]
}

model OrderItem {
  id        Int      @id @default(autoincrement())
  orderId   Int
  productId Int
  quantity  Int
  price     Float    // Price at the time of purchase

  order     Order    @relation(fields: [orderId], references: [id])
  product   Product  @relation(fields: [productId], references: [id])
}

model Settings {
  id        Int      @id @default(1)
  usdtRate  Float    @default(0.0)
  updatedAt DateTime @updatedAt
}
